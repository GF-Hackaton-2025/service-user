server:
  port: ${PORT:8080}

spring:
  application:
    name: service-user
  data:
    mongodb:
      uri: mongodb+srv://${MONGO_DB_USER}:${MONGO_DB_PASSWORD}@users.vwnkaua.mongodb.net/user?retryWrites=true&w=majority&appName=users